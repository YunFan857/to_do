# 📝 我的待办

> 一个简洁优雅的**本地待办清单**，单文件 HTML 应用，专为个人日常使用设计。

## ✨ 特性

- **五类待办，按周期管理**
  - 📌 **一次性** — 有截止日期的任务，自动标记过期
  - ☀️ **每日** — 每天循环，自动统计「连续打卡」
  - 📅 **每周** — 按指定星期循环
  - 🗓️ **每月** — 按指定日期循环
  - 🎂 **每年** — 按指定月日循环
- **圆角小卡片** — 清新现代的视觉设计
- **🌏 首都时间** — 可切换中国 / 美国，实时显示北京或华盛顿特区时间，并自动适配夏令时
- **本地存储** — 数据保存到 macOS 本地应用数据文件，不依赖浏览器缓存
- **多项目正计时** — 同时追踪多个项目，用横向时间轴记录节点并自动计算阶段间隔
- **数据备份** — 一键导出 / 导入 JSON 备份
- **🔔 定时提醒** — 提醒跟随当前选择的首都时区，到点后播放声音 + 弹出系统通知 + 顶部横幅
  - **5 种提示音可选**（每种均超过 2 秒）：柔和钟声 / 清脆叮咚 / 上行旋律 / 电子提示 / 和弦长鸣
  - **系统通知**：开启后提醒会在电脑桌面右上角弹出原生通知
  - **稍后提醒**：弹窗支持「5 分钟后再提醒」
- **键盘快捷键** — `N` 新建、`Esc` 关闭

## 🚀 使用方式

### 桌面图标（macOS，已生成）

直接双击桌面上 **「我的待办」** 应用图标即可启动。

### 直接打开

双击 `index.html`，浏览器自动打开。

### 本地开发

```bash
cd path/to/to_do
python3 -m http.server 8000
# 浏览器访问 http://localhost:8000
```

## ⌨️ 快捷键

| 按键 | 功能 |
| --- | --- |
| `N` | 新建待办（焦点不在输入框时） |
| `Esc` | 关闭弹窗 |
| `Space` / `Enter` | 切换完成状态（卡片聚焦时） |

## 🔔 提醒功能

鼠标悬停任意卡片，点击右上角 **🔕** 按钮即可设置提醒。点击顶栏 **⚙️ 设置** 可选择提示音和管理系统通知权限。

- 一次性：到 `截止日期` 的指定时间提醒一次
- 每日：每天到点提醒，完成当日打卡后当天不再响
- 每周：每周指定星期几到点提醒
- 每月：每月指定日到点提醒
- 每年：每年指定月日到点提醒

到点时会同时触发：
- 播放一段提示音（Web Audio 合成，无需音频文件，可在「⚙️ 设置」中选择 5 种风格，每种均超过 2 秒）
- 弹出系统通知（需授权浏览器通知权限，开启后即使浏览器在后台也会在桌面右上角弹出）
- 页面顶部滑下增强横幅（始终可见，不受权限影响，支持「查看 / 完成 / 5分钟后」三种操作）

提醒时间跟随页面顶部选择的时区：选择中国时，`10:00` 代表北京时间上午 10 点；选择美国时，`10:00` 代表华盛顿特区时间上午 10 点。切换时区后，已有提醒会立即按新时区重新排程；华盛顿特区的夏令时会自动处理。

首次设置提醒时会请求通知权限，**允许后系统通知才会在后台弹出**。
> 浏览器 / 系统通知在 Safari 中对 `file://` 站点支持有限，建议使用 Chrome / Edge / Brave 等以获得完整体验。

## 📁 项目结构

```
to_do/
├── index.html          # 页面应用（HTML + CSS + JS 全内联）
├── local_server.py     # 本机数据接口与静态文件服务
├── 启动我的待办.command   # 手动启动本地服务的辅助脚本
├── README.md           # 本文件
└── .gitignore          # Git 忽略规则
```

## 🛠 技术栈

- 纯 HTML / CSS / JavaScript
- **零依赖** — 无 npm、无打包工具
- macOS 本地 JSON 文件持久化
- 响应式布局，桌面 / 平板 / 手机皆可

## 📦 数据存储

桌面应用会启动一个仅监听本机的本地服务，并打开本地页面；所有待办保存到：

```text
~/Library/Application Support/我的待办/data.json
```

因此清理 Chrome / Safari 的浏览器数据不会清空待办。首次使用新版桌面应用时，会自动把旧版 `file://` 页面中的待办迁移到这个文件。直接双击 `index.html` 时如果本地服务未启动，会降级使用浏览器临时存储；请通过桌面上的「我的待办」应用图标启动，才能使用本地文件存储。

## 🔄 备份与恢复

- **导出**：顶部「导出」→ 下载 `todo-backup-YYYY-MM-DD.json`
- **导入**：顶部「导入」→ 选择备份文件 → 确认覆盖

## 📝 版本

- **当前版本**：`v2.0.0` — 工作台风格改版：左侧固定侧边栏导航 + 右侧多页面切换，新增首页仪表盘（今日完成进度、各板块统计、临近备忘、项目计时、今日速览），「一次性待办」更名为「备忘录」，视觉改为奶油底 + 柔和色卡
- `v1.7.0` — 提示音升级为 5 种可选风格（每种 ≥2 秒），新增设置弹窗，优化提醒横幅与系统通知
- `v1.6.0` — 支持多个计时项目，进展节点改为横向时间轴
- `v1.5.0` — 新增项目正计时与进展节点时间轴
- `v1.4.1` — 待办提醒改为跟随所选首都时区，并在切换时自动重新排程
- `v1.4.0` — 新增中国 / 美国时区选择，实时显示对应首都时间
- `v1.3.0` — 新增每年待办，支持按月日循环和年度提醒
- `v1.2.0` — 待办改为 macOS 本地文件持久化，不受浏览器数据清理影响
- `v1.1.0` — 新增定时提醒功能（声音 + 系统通知 + 页面横幅）
- `v1.0.1` — 初始化发布（README + .gitignore）
- 详见 [Releases](../../releases)

## 📄 License

MIT
